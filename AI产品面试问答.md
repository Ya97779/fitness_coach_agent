# AI 产品经理面试问答

> 面试岗位：TikTok 业务风控 AI 产品经理
> 日期：2026-05-14

---

## Q1：你做的这个产品打算如何推广

FitCoach AI 的推广我会分三个阶段：

**第一阶段：种子用户验证（0-1000人）**

先在健身社群做小范围测试。找 3-5 个健身博主/KOC，免费提供产品体验，换取真实使用反馈和内容产出。这个阶段核心不是量，是验证用户愿不愿意持续用——次日留存和 7 日留存比注册数重要。

**第二阶段：微信生态裂变**

产品本身是微信小程序，天然适合社交传播。设计两条裂变路径：
- 内容驱动：用户生成「我的健身周报」海报，分享到朋友圈
- 功能驱动：邀请好友解锁高级功能（比如更详细的营养分析）

小程序的获客成本比 App 低很多，这是我们的结构性优势。

**第三阶段：内容矩阵**

在小红书、抖音做健身科普内容，把产品作为「AI 教练」的案例植入。不硬推产品，而是让用户看到效果后自然搜索。

**核心判断：** 健身类产品推广的关键不是曝光量，是信任。用户把自己的身体数据交给一个产品，决策门槛很高。所以我会优先做口碑和真实案例，而不是砸钱买量。

---

## Q2：假如像 Keep 这种大平台也复制你的产品，你如何做差异化，如何竞争

先说一个前提：大厂抄你，说明方向对了。这是好事不是坏事。

**大厂的结构性劣势，就是我的机会**

Keep 的核心商业模式是卖课程和会员。AI 真正个性化之后，会冲击它的课件收入结构——用户不需要买固定课程了，AI 按身体数据实时生成方案。Keep 不会革自己的命，这是创新者的窘境。

**差异化策略：**

**1. 深度个性化 vs 通用方案**

Keep 的 AI 如果做，大概率是「课程推荐」——在已有课库挑一个给你。我的产品是「方案生成」——根据你的体重、目标、饮食偏好、训练历史，从零生成专属方案。这不是功能差异，是产品基因差异。

**2. 营养 + 训练一体化**

Keep 强在运动，弱在饮食。我的产品天然双 Agent 架构，训练和饮食联动。今天练了腿，晚上自动调整蛋白质摄入建议。大厂两个团队做两个功能，协调成本很高。

**3. 垂直深度 vs 平台广度**

Keep 要服务 3 亿用户，只能做通用。我可以聚焦细分人群——比如「备孕女性体重管理」「糖尿病患者运动方案」——这些场景专业壁垒高，大厂不愿投入，但用户付费意愿强。

**最后一点坦诚：** 如果大厂真的全力投入，创业公司正面硬刚是打不过的。所以关键是速度——在大厂反应过来之前，跑出数据壁垒和用户口碑，成为细分领域的第一选择。等它来抄的时候，用户已经在你这积累了半年的训练数据，迁移成本已经建立起来了。

---

## Q3：你整个 RAG 系统是如何设计的，如何评估 RAG 系统的效果

### RAG 系统设计

整体是「多路召回 + 混合检索 + 生成增强」的架构。

**知识库构建：** 健身、营养领域的专业文档，启动时做增量索引，MD5 对比避免重复处理，存入 ChromaDB 向量库。

**检索层做了三层优化：**

- **BM25 关键词检索**：用 jieba 中文分词，解决专业术语的精确匹配问题。比如「深蹲膝盖能过脚尖吗」，纯向量检索可能召回不相关内容，BM25 能精准命中。
- **向量语义检索**：用智谱 embedding-2 做语义匹配，处理用户表述多样化的问题。
- **混合排序（RRF）**：两路结果用倒数排名融合算法合并，兼顾精确性和语义相关性。

**生成增强：**

- **HyDE**：先让 LLM 生成一个假设性答案，用这个假设答案去做检索，比直接用用户问题检索效果好。
- **Self-RAG**：生成答案后，模型自己判断「这个回答需不需要检索支持」，避免对简单问题过度检索，也避免对复杂问题胡编乱造。

### 评估体系

评估 RAG 效果我分两层：

**离线评估：**
- 准备标注数据集，包含问题、标准答案、相关文档
- 指标看三个：**召回率**（相关文档有没有被检索到）、**精确率**（召回的文档里有多少是相关的）、**答案忠实度**（生成的答案是否忠于检索到的文档，没有幻觉）
- 用 `test_rag_evaluation` 脚本批量跑，支持 `--quick` 快速 5 条和全量评估，结果导出 JSON 报告

**线上评估：**
- 用户隐式反馈：追问率、重新提问率（说明第一次没答好）
- 抽样人工评估：每周抽 50 条对话，人工打分

**一个关键判断：** RAG 的效果瓶颈通常不在模型，在于知识库质量和检索策略。我会把 70% 的精力放在数据清洗和 chunk 切分策略上，而不是换更大的模型。

---

## Q4：你用的什么大模型，为什么要使用这个模型而不是别的模型，AI 产品如何选择适合的模型

### 当前选择：智谱 GLM-4.7

通过 OpenAI 兼容接口调用，用的是智谱的 `open.bigmodel.cn`。

**为什么选它，而不是 GPT-4 或 Claude：**

**1. 中文场景优势**

健身营养领域的知识库是中文，用户提问是中文，GLM 在中文理解和生成上和 GPT-4 差距不大，但在中文专业术语的处理上更稳定。比如「碳水循环」「超量恢复」这类术语，国产模型的训练语料覆盖更充分。

**2. 成本结构**

健身顾问的对话轮次多，每轮都要调 LLM。GPT-4 的 token 单价是 GLM 的 5-8 倍，同样的用户体验，成本差一个数量级。创业阶段，这个账必须算清楚。

**3. 合规和数据安全**

用户的身体数据属于敏感信息，数据出境有法律风险。用国产模型，数据在国内，合规成本低很多。

**4. 接口兼容性**

智谱提供 OpenAI 兼容接口，代码层面几乎零迁移成本。如果未来要换模型，改动很小。

### AI 产品如何选模型，决策框架

| 维度 | 核心问题 |
|------|---------|
| 任务匹配度 | 你的任务需要推理能力还是语言流畅度？代码生成还是中文对话？ |
| 成本 | 日均调用量多少？单次对话 token 消耗多少？毛利能不能覆盖？ |
| 延迟 | 用户能接受几秒等待？流式输出是否必须？ |
| 合规 | 数据能不能出境？行业有没有特殊监管？ |
| 可替换性 | 有没有做抽象层？换模型要改多少代码？ |

**一个原则：** 不选最强的模型，选最合适的模型。产品里简单问题用低 temperature 的快速模型，复杂推理用高 temperature 的强模型，而不是所有请求都打同一个模型。

---

## Q5：你的项目为什么要做成多 Agent 的架构，而不是单 Agent

**核心原因：单一 Agent 在复杂任务上会降智。**

一个 Agent 要同时做意图识别、营养查询、训练方案生成、答案质量把控，prompt 会变得又长又乱。模型的注意力是有限的，塞太多指令进去，每件事都做不好。

**多 Agent 的设计逻辑是「专业分工 + 质量兜底」：**

**Router Agent — 门卫**
用户说「我今天吃什么」和「帮我做个训练计划」，需要的工具链、知识库、回答策略完全不同。Router 做意图分类，把请求分发到对应的专业 Agent。关键词匹配先兜底，LLM 分类兜复杂情况，保证分发准确率。

**Nutrition Agent — 营养专家**
接入食物营养 API 和营养知识库，有自己专属的工具集。它不需要懂训练，只需要把饮食这件事说清楚。

**Fitness Agent — 训练专家**
接入训练知识库，专注运动方案。和 Nutrition Agent 的工具链、检索策略、prompt 人格都不同。

**Expert Agent — 评审官**
这是关键设计。前面的 Agent 生成答案后，Expert 打分。低于 3 分就打回去重做，最多重试 3 次。相当于内置了一个 QA 流程。

**单 Agent 做不到的事：**

- **独立评估**：自己审自己，模型很难客观。必须有一个独立的角色来做质量把控。
- **工具隔离**：营养 Agent 有食物 API，训练 Agent 没有。单 Agent 所有工具混在一起，模型可能在不该调 API 的时候调了。
- **prompt 解耦**：每个 Agent 的 system prompt 只包含自己领域的指令和知识，context 更干净，输出质量更高。

**一个取舍：** 多 Agent 会增加延迟和 token 消耗，因为有额外的路由调用和评审调用。但我们的场景是健身顾问，用户能接受 2-3 秒等待，这个 tradeoff 是值得的。如果是实时聊天场景，可能就要重新考虑了。

---

## Q6：你的健身 Agent 的整体项目架构是什么样的，为什么要这么设计

**整体架构分四层：**

```
交互层：Streamlit 前端
    ↓ SSE 流式
接入层：FastAPI + JWT 鉴权
    ↓
编排层：LangGraph StateGraph（Router → Agent → Expert Review）
    ↓
能力层：LLM / RAG / 外部API / 记忆系统 / 数据库
```

**为什么要这么分层：**

**1. 交互层选 Streamlit，核心考量是验证速度**

创业阶段最重要的是验证产品价值，不是前端体验。Streamlit 单文件就能搞定聊天、用户档案、数据统计三个页面，一个人一天就能出原型。等产品验证跑通了，再换成 React 或小程序原生前端，后端接口不用改。

**2. 接入层做 SSE 流式，因为体验刚需**

用户问一个问题，如果要等 5 秒才出完整答案，流失率会很高。SSE 流式输出让用户看到文字一个字一个字蹦出来，感知等待时间大幅缩短。而且架构里有一个 expert review 环节，流式输出时跳过评审，先给用户看初版答案，保证实时性。

**3. 编排层用 LangGraph 而不是手写 if-else**

手写调度逻辑，Agent 之间的状态传递、重试机制、条件分支会变成 spaghetti code。LangGraph 是声明式的，定义节点和边，状态流转清晰可维护。而且 LangGraph 原生支持 checkpoint，出问题可以回溯到任意节点。

**4. 能力层做单例和缓存，控制成本**

- LLM 实例按 temperature 缓存，同一个 temperature 不会重复创建连接
- RAG 全局单例，ChromaDB 连接只初始化一次
- 启动时增量索引，不每次全量重建

**5. 记忆系统独立出来，这是产品壁垒**

用户说「我乳糖不耐受」，下一次对话营养 Agent 必须知道这件事。记忆系统维护用户画像、对话摘要、统计数据，每次对话注入 prompt。这个数据积累越多，产品越难被替代。

**一个架构决策的反思：** 数据库选了 SQLite，前期够用，但如果要上多实例部署，需要迁移到 PostgreSQL。这个是技术债，但现阶段不是瓶颈，先扛着。

---

## Q7：你的 Agent 是如何调用 Tool 的

**核心机制：LLM 自主决策调用，不是硬编码。**

**工具定义：** 用 LangChain 的 `@tool` 装饰器，每个工具写清楚名称、描述、参数。工具的描述很关键——LLM 靠这个描述决定要不要调用、传什么参数。写得不好，模型就不知道什么时候该用。

**调用流程：**

```
用户问「鸡胸肉和牛肉哪个蛋白质更高」
  → Nutrition Agent 收到问题 + 工具列表
  → LLM 判断：需要查两个食物的营养数据
  → LLM 输出：tool_call(search_food_nutrition, "鸡胸肉")
              tool_call(search_food_nutrition, "牛肉")
  → 框架执行工具，把结果返回给 LLM
  → LLM 拿到数据，生成对比答案
```

**关键设计点：**

**1. 工具隔离**

Nutrition Agent 有食物 API 工具和营养知识库检索工具，Fitness Agent 有训练知识库检索工具。不是所有工具都给所有 Agent，而是在定义 Graph 节点时绑定。这样 LLM 不会在回答训练问题时误调食物 API。

**2. RAG 也是一种工具**

知识库检索不是在 Agent 外部做的，而是作为 tool 暴露给 LLM。模型自己判断「这个问题需不需要查知识库」。Self-RAG 的逻辑就在这里——模型先判断是否需要检索，需要才调用检索工具。

**3. LLM 可以一次调多个工具**

如果问题涉及多个食物或者多个动作，模型可以并行发出多个 tool call，框架批量执行后一起返回，减少交互轮次。

**4. 工具调用失败有兜底**

天行 API 挂了或者超时，有本地兜底数据。不是直接报错给用户，而是降级返回基础营养数据。

**一个产品判断：** 工具设计的原则是「宁可少给，不要多给」。工具太多，模型选择困难，调用准确率下降。每个 Agent 持有 3-5 个核心工具就够了。

---

## Q8：对风控的理解

**风控的本质：在不伤害正常用户的前提下，识别和拦截恶意行为。**

风控不是「拦截越多越好」，而是找一个最优平衡点——漏放的坏人要少，误伤的好人也要少。这两个指标天然矛盾，产品经理的核心价值就是在这个矛盾里做取舍。

**TikTok 的风控场景，有几层：**

**第一层：内容风控**
- 违规内容识别：色情、暴力、政治敏感、虚假信息
- 搬运和抄袭检测：视频指纹、音频匹配
- AI 生成内容识别：深度伪造、AIGC 标注

这一层的核心挑战是「边界模糊」。擦边内容怎么定义？不同国家文化标准不同？误判一条正常视频可能让一个创作者流失。

**第二层：行为风控**
- 刷量、刷赞、刷粉
- 批量注册、养号
- 机器人流量、虚假互动
- 羊毛党薅补贴

这一层的核心挑战是「对抗升级」。黑产也在用 AI，你出一个检测策略，他一周内就能绕过。风控是持续对抗，不是一劳永逸。

**第三层：商业风控**
- 广告流量欺诈
- 电商刷单
- 支付和交易风险

**对风控产品的几个理解：**

**1. 风控是成本中心，但也是平台的护城河**

用户在你这看到的内容质量、交易安全感，直接决定留存。没有好的风控，劣币驱逐良币，平台生态会崩。

**2. 策略比模型重要**

模型是工具，策略才是核心。什么是违规？阈值定多少？触发后怎么处置（限流、删除、封号、人工复审）？这些决策需要产品来定，不是算法来定。

**3. 误杀的代价往往比漏放更大**

漏放一条违规内容，影响有限。误杀一个头部创作者，他可能直接去竞品了。所以处置策略要有梯度，不能一刀切。

**4. 风控需要「可观测性」**

每个策略上线后，拦截了多少、误杀了多少、黑产绕过了没有，必须有实时数据看板。没有数据支撑的风控就是在盲打。

---

## Q9：在风控场景中如何进行数据分析，如何设计科学合理的指标

**风控数据分析的核心挑战：你没有 ground truth。**

你不知道你没拦住的坏人有多少，也不知道你误杀的好人有多少。所以指标设计的核心是「用可观测的代理指标逼近真实效果」。

**一、衡量处置有效性：这条规则有没有用**

| 指标 | 定义 | 怎么用 |
|------|------|--------|
| 拦截率 | 触发策略的请求中，真正违规的比例 | 核心指标，低于阈值说明策略太松，误杀太多 |
| 漏放率 | 事后被确认违规但未被拦截的比例 | 抽样人工标注估算，不可能精确，但趋势有意义 |
| 黑产绕过率 | 新策略上线后，黑产行为模式变化的速度 | 如果7天内拦截量断崖下降，说明被绕过了 |
| 处置转化率 | 被处置账号中，二次违规的比例 | 二次违规率低说明处置有效，高说明处置太轻或者误杀了 |

**关键方法：分层抽样 + 人工标注。** 纯靠自动指标会自欺欺人，必须定期抽样人工验证。

**二、评估治理效果：整体风控体系健不健康**

**漏斗视角：**

```
总请求量
  → 命中风控规则量（触发率）
    → 自动处置量（自动处置率）
      → 人工复审量（复审率）
        → 确认违规量（命中率）
          → 申诉量（申诉率）
            → 申诉成功量（误判率）
```

每一层的转化率都有意义：

- **触发率太高** → 规则太激进，正常用户被拦截，影响体验
- **自动处置率太高** → 缺少兜底，误杀风险大
- **复审率太高** → 自动化不够，人力成本高
- **申诉成功率太高** → 策略有系统性偏差，必须调整

**时间序列视角：**

单点数据没意义，要看趋势。核心是建立「攻防节奏」的感知：

- 新策略上线前后对比
- 黑产活动的周期性规律（大促前、补贴期间）
- 不同国家/地区的差异趋势

**三、评估业务影响：风控对平台生态的影响**

**对正常用户的影响：**
- 风控拦截后的用户流失率：被误拦截的用户，有多少不再活跃了
- 新用户注册转化率：风控越严，注册流程越复杂，转化率越低
- 内容发布量变化：如果创作者被限流后发帖量下降，说明误伤了

**对平台生态的影响：**
- 优质内容占比变化：治理前后，低质/违规内容占比的变化
- 用户信任指标：举报率、投诉率的变化
- 广告主信心：广告投放量、CPM 变化

**对商业指标的影响：**
- GMV 损失估算：拦截了多少「灰色地带」的交易
- 补贴防薅节省金额：羊毛党被拦截后少发了多少补贴
- 这个数字要说服老板继续投入风控资源

**四、一个关键方法论：A/B 实验**

风控策略必须做 A/B 实验，不能拍脑袋上线：

- 实验组走新策略，对照组走旧策略
- 核心看两个指标：违规漏放率有没有上升，正常用户指标有没有下降
- 灰度放量，逐步扩大

**最后说一个认知：** 风控数据分析最大的坑是「只看拦截量」。拦截量高不代表效果好，可能是误杀多。真正好的风控是用户感知不到的——坏人被拦住了，好人不受影响，业务指标在涨。

---

## Q10：如何理解 TikTok 的风控系统

### 四层框架：感知-识别-干预-衡量

**一、感知层：你得先知道发生了什么**

感知是风控的起点——没有数据，一切都是空谈。

- **实时数据采集**：用户行为日志、设备指纹、网络环境、内容特征、社交关系图谱
- **威胁情报**：黑产工具更新、新型作弊手法、暗网泄露数据
- **业务信号**：异常流量波动、特定地区投诉激增、某个视频的举报率突然飙升

感知层的核心挑战是「信号噪声比」。TikTok 的 DAU 是十亿级别的，每天产生的事件量是天文数字，如何从海量数据中快速捕捉到有价值的异常信号，是第一道难题。

**二、识别层：判断这是不是风险**

识别分两条路径：

**规则引擎**：确定性高的场景直接上规则。比如同一设备 1 小时内注册 50 个账号，不需要模型，规则就能拦。优点是可解释、可审计，缺点是只能抓已知模式。

**机器学习模型**：复杂场景用模型。比如判断一个视频是不是深度伪造、一个账号的行为模式是不是机器人。优点是能发现未知模式，缺点是黑箱、需要标注数据。

实际是两者结合——规则先拦截明确违规，模型处理灰色地带，人工复审兜底。

**三、干预层：发现问题后怎么处置**

干预必须有梯度，不能一刀切：

| 梯度 | 处置方式 | 适用场景 |
|------|---------|---------|
| 轻 | 限流、降权 | 疑似违规但不确定 |
| 中 | 内容下架、功能限制 | 明确违规但非恶意 |
| 重 | 封号、设备封禁 | 恶意违规、屡犯 |
| 兜底 | 人工复审 | 模型置信度低的 case |

关键原则：**可逆优先**。宁可先限流观察，也不要直接封号。误杀一个真实用户的代价远高于漏放一条违规内容。

**四、衡量层：效果好不好，用数据说话**

核心是三组指标：
- 处置有效性：拦截率、漏放率、误判率
- 治理健康度：漏斗各层转化率的时间趋势
- 业务影响：对正常用户指标和商业指标的影响

### TikTok 与国内平台的核心差异

**1. 合规性：不是一个法律，是一百多个法律**

国内平台只需要遵守中国的法律法规。TikTok 要同时满足：
- 欧盟 GDPR（数据隐私，用户有被遗忘权）
- 美国各州不同的隐私法（加州 CCPA、德州 HB 等）
- 印度、东南亚各国的内容审查标准
- 中东地区的宗教和文化敏感内容

同一个视频，在 A 国合法，在 B 国违法。风控系统必须按地区做差异化策略，而不是一套规则全球通用。这是架构层面的挑战——策略引擎要支持「国家 × 内容类型 × 用户群体」的多维配置。

**2. 用户群体分散性：你面对的不是一类人**

国内平台的用户画像相对集中，文化差异小。TikTok 的用户分布在 150 多个国家：
- **语言多样性**：同一个违规概念，不同语言的表达方式完全不同，NLP 模型需要多语言能力
- **文化差异**：在西方是正常表达，在中东可能是严重违规
- **黑产生态差异**：国内黑产用微信群组织，海外用 Telegram、暗网，对抗手段不同
- **时区差异**：攻击可能发生在你睡觉的时候，需要 7×24 自动化响应

这意味着风控团队不能只有中国工程师，需要本地化运营团队理解当地语境。

**3. 业务链路复杂性：不是一个产品，是一个生态**

国内的抖音主要是内容 + 电商。TikTok 的业务线更多：
- 短视频
- 直播（打赏、带货）
- TikTok Shop（电商）
- 广告平台
- 搜索

每条业务线有自己的风控场景，但又互相交叉。比如一个账号可能同时刷视频播放量、刷直播间人气、刷电商好评——跨业务线的关联识别是更高的技术要求。

而且 TikTok 的技术栈和抖音不完全共享，很多基建需要独立建设，风控系统也要做适配。

**总结一句话：** TikTok 风控的本质挑战不是技术，是在「一百多个法律 × 一百多种文化 × 多条业务线」的组合空间里，找到一个足够精细又足够高效的治理方案。复杂度比国内平台高一个数量级。

---

## Q11：如何设计支持不同业务的通用型风控产品

**核心思路：平台化。底层统一，上层灵活。**

不是每条业务线各建一套风控系统，而是建一个风控中台，业务线像插件一样接入。

### 一、统一的核心模块

**1. 统一特征库**

所有业务线共享同一套特征计算和存储体系：

- **用户维度特征**：注册时长、设备指纹、行为序列、社交关系图谱——这些特征跨业务线通用
- **内容维度特征**：文本、图像、音频、视频的多模态特征——一次计算，多业务复用
- **环境维度特征**：IP、地理位置、网络环境、设备状态

关键设计：特征分「全局特征」和「业务特征」。全局特征所有业务线都能用，业务特征只有特定业务线能定义和使用。特征平台提供统一的注册、计算、查询接口，业务线不需要自己造轮子。

**2. 统一规则引擎**

- 规则的表达式语法统一，业务线通过配置而非开发来定义规则
- 支持实时规则（流式计算）和离线规则（批量计算）
- 规则生命周期管理：创建 → 灰度 → 全量 → 监控 → 下线

举个例子：

```
短视频业务：同一用户1小时内点赞超过200次 → 疑似刷量
直播业务：同一用户1小时内进入直播间超过50个 → 疑似引流
电商业务：同一设备下单超过10笔不同地址 → 疑似刷单
```

底层是同一套规则引擎，业务线只是填不同的配置。

**3. 统一模型服务**

- 通用模型下沉到中台：垃圾文本识别、图像违规检测、账号风险评分——这些能力跨业务通用
- 业务模型由中台提供训练框架和部署平台，业务线提供数据和标签
- 模型统一做版本管理、A/B 实验、效果监控

**4. 统一处置中心**

处置动作标准化为有限的原子操作：限流、下架、封号、打标、人工复审队列。业务线组合这些原子操作定义自己的处置策略，而不是各写各的封号逻辑。

### 二、业务个性化配置入口

通用模块解决 80% 的问题，剩下 20% 是业务特有的：

**配置化而非代码化：**

| 配置项 | 说明 |
|--------|------|
| 风控场景注册 | 业务线定义自己的场景（如「直播间刷人气」），绑定对应的特征和规则 |
| 策略优先级 | 不同业务线可以定义不同的拦截阈值和处置力度 |
| 复审队列分配 | 高风险 case 分配给哪个审核团队，按业务线配置 |
| 告警规则 | 什么级别的异常通知谁，各业务线不同 |
| 灰度比例 | 新策略在各业务线的放量节奏独立控制 |

**配置后台的用户体验很关键：**

运营同学不是工程师，配置界面要可视化——拖拽式规则编排、实时预览拦截效果、一键回滚。降低业务线接入风控的门槛，才能真正推广起来。

### 三、统一的效果衡量体系

各业务线的指标定义必须对齐，否则没法横向比较和资源分配：

**统一指标定义：**

- **安全指标**：拦截率、漏放率、误判率——定义和计算口径全公司统一
- **效率指标**：平均处置时长、人工复审占比、策略上线周期
- **业务指标**：各业务线的核心指标（DAU、GMV、直播时长）与风控事件的关联分析

**统一数据看板：**

- **全局视角**：CISO 看全平台风险态势，各业务线的风控健康度排名
- **业务视角**：业务负责人看自己业务线的详细数据
- **策略视角**：风控 PM 看每条策略的实时效果

**统一的效果评估流程：**

新策略上线必须经过：
1. 离线评估：用历史数据回放，看拦截率和误判率
2. 小流量灰度：真实流量小范围验证
3. A/B 实验：实验组 vs 对照组，核心指标对比
4. 全量上线 + 持续监控

**总结：** 通用风控产品的价值在于「一次建设，N 个业务复用」。核心模块统一降低研发成本，配置化接入降低业务线的使用门槛，统一衡量体系让风控效果可比较、可决策。最终目标是业务线接入风控从「要开发三个月」变成「配置一下就上线」。
